import { useCallback, useState } from "react";

const useQueue = (initialData = []) => {
  

  return {
    push,
    pop,
    clear,
    top: ,
    size: ,
    queue,
  };
};

export default useQueue;
